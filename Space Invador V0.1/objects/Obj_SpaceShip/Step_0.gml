/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur

// creation des "munitions"
if(Obj_input.space){
	instance_create_layer(x,y,"Instances",Obj_bullet);
}
